app:
  src:
    - lib/**/*.js
    - "!lib/app.js"
    - "!lib/api/auth.js"
  directives:
    node: true
    unparam: true
    nomen: true
  options:
    errorsOnly: true

test:
  src:
    - test/**/*.js
    - "!test/integration/**"
    - "!test/unit/api/auth-test.js"
  directives:
    node: true
    regexp: true
    nomen: true
    unparam: true
    predef:
      - suite
      - setup
      - teardown
      - test
      - assert
      - refute
      - sinon
  options:
    errorsOnly: true
