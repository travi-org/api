lint: eslint

unit: mocha_istanbul:unit

integration: cucumberjs:integration
wip: cucumberjs:wip

test:
  - unit
  - integration

compile: babel

build:
  - compile
  - flyway:migrate

heroku:production: build

default:
  - lint
  - test
