lint: eslint

unit: mochacov:unit

integration: cucumberjs:integration
wip: cucumberjs:wip

test:
  - unit
  - integration

build: flyway:migrate

heroku:production: build

default:
  - lint
  - test
