options:
  driver: org.postgresql.Driver
  url: 'jdbc:<%= db.url %>'
  user: '<%= db.user %>'
  password: '<%= db.password %>'
  locations: 'filesystem:data/migrations'

validate:
  command: validate

migrate:
  command: migrate
